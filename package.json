{
  "name": "verisite-render-service",
  "version": "1.0.0",
  "private": true,
  "description": "Micro-service de rendu headless pour VeriSite : charge une page avec JavaScript et renvoie le HTML final, les cookies réellement déposés et les requêtes réseau (état AVANT consentement).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup": "playwright install --with-deps chromium"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "1.45.0"
  }
}
